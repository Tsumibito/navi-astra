---
import { renderSiteFooter } from '../lib/site-shell.mjs';
interface Props { locale?: 'ru' | 'ua' | 'en' }
const { locale = 'ru' } = Astro.props as Props;
---
<Fragment set:html={renderSiteFooter(locale)} />
